[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "codedifferently_instructional"
version = "0.1.0"

[tool.setuptools]
packages = ["codedifferently_instructional"]